const admins = document.querySelector("#admins"), statusNode = document.querySelector("#status");
async function load() { const response = await fetch("/api/admin-access/admins"); if (!response.ok) return location.assign("/admin/"); const { items } = await response.json(); admins.innerHTML = `<p>${items.map((item) => `${escapeHtml(item.username)} · ${escapeHtml(item.role)}${item.last_login_at ? " · zuletzt aktiv" : ""}`).join("<br />")}</p>`; }
document.querySelector("#createAdminForm").addEventListener("submit", async (event) => { event.preventDefault(); const response = await fetch("/api/admin-access/admins", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ username: document.querySelector("#newUsername").value, password: document.querySelector("#newPassword").value }) }); const result = await response.json(); statusNode.textContent = response.ok ? "Admin angelegt." : (result.message || "Admin konnte nicht angelegt werden."); if (response.ok) { event.target.reset(); load(); } });
function escapeHtml(value) { const element = document.createElement("span"); element.textContent = value; return element.innerHTML; } load();
