worker_processes auto;
pid /tmp/nginx.pid;

events {
  worker_connections 1024;
}

http {
  log_format gernetix_no_query '$remote_addr - $remote_user [$time_local] '
    '"$request_method $uri $server_protocol" $status $body_bytes_sent '
    '"$http_user_agent"';
  access_log /dev/stdout gernetix_no_query;
  error_log /dev/stderr warn;

  upstream gernetix_build_workers {
    least_conn;
    include /var/run/gernetix/build-workers.inc;
  }

  server {
    listen 4400;
    client_max_body_size 10m;

    location = /router-health {
      access_log off;
      return 200 "ok\n";
    }

    location / {
      proxy_pass http://gernetix_build_workers;
      proxy_http_version 1.1;
      proxy_set_header Host $host;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_connect_timeout 5s;
      proxy_read_timeout 1h;
      proxy_send_timeout 1h;
      proxy_next_upstream error timeout http_502 http_503 http_504;
      proxy_next_upstream_tries 3;
    }
  }
}
