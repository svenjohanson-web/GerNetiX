limit_req_zone $binary_remote_addr zone=gernetix_web_per_ip:10m rate=10r/s;
limit_req_zone $binary_remote_addr zone=gernetix_auth_per_ip:10m rate=5r/m;
limit_req_zone $binary_remote_addr zone=gernetix_build_per_ip:10m rate=30r/s;
limit_conn_zone $binary_remote_addr zone=gernetix_connections_per_ip:10m;
limit_req_status 429;
limit_conn_status 429;

server {
  listen 8443 ssl;
  server_name _;

  ssl_certificate /etc/letsencrypt/live/gernetix.nl/fullchain.pem;
  ssl_certificate_key /etc/letsencrypt/live/gernetix.nl/privkey.pem;
  ssl_protocols TLSv1.2 TLSv1.3;
  add_header Strict-Transport-Security "max-age=31536000" always;

  client_max_body_size 10m;
  limit_conn gernetix_connections_per_ip 30;

  # The installable operator PWA and its push registration are deliberately
  # private. The public web edge must not reveal the app shell to scanners.
  location ^~ /app/ {
    allow 10.77.0.0/24;
    deny all;
    proxy_pass http://identity-server:4300;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto https;
  }

  location ^~ /api/push/ {
    allow 10.77.0.0/24;
    deny all;
    proxy_pass http://identity-server:4300;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto https;
  }

  location ~ ^/api/(login(?:/external)?|register)$ {
    limit_req zone=gernetix_auth_per_ip burst=5 nodelay;
    proxy_pass http://identity-server:4300;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto https;
  }

  location / {
    limit_req zone=gernetix_web_per_ip burst=40 nodelay;
    proxy_pass http://identity-server:4300;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto https;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";
  }
}

server {
  listen 8443 ssl;
  server_name gernetix.nl www.gernetix.nl;
  ssl_certificate /etc/letsencrypt/live/gernetix.nl/fullchain.pem;
  ssl_certificate_key /etc/letsencrypt/live/gernetix.nl/privkey.pem;
  ssl_protocols TLSv1.2 TLSv1.3;
  add_header Strict-Transport-Security "max-age=31536000" always;
  limit_conn gernetix_connections_per_ip 20;
  limit_req zone=gernetix_web_per_ip burst=20 nodelay;
  root /usr/share/nginx/html;
  location ^~ /vendor/esptool-js/ { proxy_pass http://public-demo-server:4920/vendor/esptool-js/; }
  location = /demos { return 301 /demos/; }
  location ^~ /demos/api/ { proxy_pass http://public-demo-server:4920/api/; proxy_http_version 1.1; proxy_set_header Host $host; proxy_set_header X-Forwarded-Proto https; }
  location ^~ /demos/ { proxy_pass http://public-demo-server:4920/; proxy_http_version 1.1; proxy_set_header Host $host; proxy_set_header X-Forwarded-Proto https; }
  location = / { try_files /welcome.nl.html =404; }
  location = /gernetix-logo.png { try_files /gernetix-logo.png =404; }
  location / { return 404; }
}

server {
  listen 8443 ssl;
  server_name gernetix.de www.gernetix.de;
  ssl_certificate /etc/letsencrypt/live/gernetix.nl/fullchain.pem;
  ssl_certificate_key /etc/letsencrypt/live/gernetix.nl/privkey.pem;
  ssl_protocols TLSv1.2 TLSv1.3;
  add_header Strict-Transport-Security "max-age=31536000" always;
  limit_conn gernetix_connections_per_ip 20;
  limit_req zone=gernetix_web_per_ip burst=20 nodelay;
  root /usr/share/nginx/html;
  location ^~ /vendor/esptool-js/ { proxy_pass http://public-demo-server:4920/vendor/esptool-js/; }
  location = /demos { return 301 /demos/; }
  location ^~ /demos/api/ { proxy_pass http://public-demo-server:4920/api/; proxy_http_version 1.1; proxy_set_header Host $host; proxy_set_header X-Forwarded-Proto https; }
  location ^~ /demos/ { proxy_pass http://public-demo-server:4920/; proxy_http_version 1.1; proxy_set_header Host $host; proxy_set_header X-Forwarded-Proto https; }
  location = / { try_files /welcome.html =404; }
  location = /gernetix-logo.png { try_files /gernetix-logo.png =404; }
  location / { return 404; }
}

server {
  listen 8443 ssl;
  server_name gernetix.com www.gernetix.com;
  ssl_certificate /etc/letsencrypt/live/gernetix.nl/fullchain.pem;
  ssl_certificate_key /etc/letsencrypt/live/gernetix.nl/privkey.pem;
  ssl_protocols TLSv1.2 TLSv1.3;
  add_header Strict-Transport-Security "max-age=31536000" always;
  limit_conn gernetix_connections_per_ip 20;
  limit_req zone=gernetix_web_per_ip burst=20 nodelay;
  root /usr/share/nginx/html;
  location ^~ /vendor/esptool-js/ { proxy_pass http://public-demo-server:4920/vendor/esptool-js/; }
  location = /demos { return 301 /demos/; }
  location ^~ /demos/api/ { proxy_pass http://public-demo-server:4920/api/; proxy_http_version 1.1; proxy_set_header Host $host; proxy_set_header X-Forwarded-Proto https; }
  location ^~ /demos/ { proxy_pass http://public-demo-server:4920/; proxy_http_version 1.1; proxy_set_header Host $host; proxy_set_header X-Forwarded-Proto https; }
  location = / { try_files /welcome.en.html =404; }
  location = /gernetix-logo.png { try_files /gernetix-logo.png =404; }
  location / { return 404; }
}

server {
  listen 8443 ssl;
  server_name build.gernetix.com;
  ssl_certificate /etc/letsencrypt/live/gernetix-services.com/fullchain.pem;
  ssl_certificate_key /etc/letsencrypt/live/gernetix-services.com/privkey.pem;
  ssl_protocols TLSv1.2 TLSv1.3;
  add_header Strict-Transport-Security "max-age=31536000" always;
  client_max_body_size 10m;
  limit_conn gernetix_connections_per_ip 50;

  location / {
    limit_req zone=gernetix_build_per_ip burst=100 nodelay;
    proxy_pass http://build-deploy-server:4400;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto https;
  }
}

server {
  listen 8443 ssl;
  server_name pwa.gernetix.com;
  ssl_certificate /etc/letsencrypt/live/gernetix-services.com/fullchain.pem;
  ssl_certificate_key /etc/letsencrypt/live/gernetix-services.com/privkey.pem;
  ssl_protocols TLSv1.2 TLSv1.3;
  add_header Strict-Transport-Security "max-age=31536000" always;
  client_max_body_size 10m;
  limit_conn gernetix_connections_per_ip 20;

  # The private PWA is intentionally unreachable from the public Internet.
  # WireGuard peers use the assigned 10.77.0.0/24 address range.
  location ^~ /admin/ {
    allow 10.77.0.0/24;
    deny all;
    limit_req zone=gernetix_auth_per_ip burst=5 nodelay;
    proxy_pass http://admin-access-server:4610;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto https;
  }

  location ^~ /api/admin-access/ {
    allow 10.77.0.0/24;
    deny all;
    limit_req zone=gernetix_auth_per_ip burst=5 nodelay;
    proxy_pass http://admin-access-server:4610;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto https;
  }

  location ^~ /api/admin/ {
    allow 10.77.0.0/24;
    deny all;
    proxy_pass http://admin-access-server:4610;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto https;
  }

  location / {
    allow 10.77.0.0/24;
    deny all;
    limit_req zone=gernetix_web_per_ip burst=20 nodelay;
    proxy_pass http://identity-server:4300;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto https;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";
  }
}
